
 
 {#
| Variable               | Type                  | Description                                                                 |
|------------------------|-----------------------|-----------------------------------------------------------------------------|
| component              | object                | يحتوي على إعدادات التاجر للحقول من قسم `component` في `twilight.json`   |
| component.background   | string                | رابط صورة الخلفية (إذا كانت موجودة)                                       |
| component.title        | ?string               | العنوان الرئيسي (إذا كان موجودًا)                                           |
| component.sub_title    | ?string               | العنوان الفرعي (إذا كان موجودًا)                                           |
| component.button_text  | ?string               | نص الزر (إذا كان موجودًا)                                                   |
| component.button_url   | ?string               | رابط الزر (إذا كان موجودًا)                                                |
| position               | int                   | رقم ترتيب المكون (يبدأ من 0)                                              |
#}



<section class="s-block s-block--fixed-banner bg-gray-100 py-12 relative text-center" style="background-image: url('{{ component.image }}'); background-size: cover; background-position: center;">
    <div class="container mx-auto px-4 relative z-10">
  
      {# إذا كان العنوان موجودًا #}
      {% if component.title %}
        <h2 class="text-3xl font-bold text-white mb-2">
          {{ component.title }}
        </h2>
      {% endif %}
  
      {# إذا كان العنوان الفرعي موجودًا #}
      {% if component.sub_title %}
        <p class="text-lg text-white mb-6">
          {{ component.sub_title }}
        </p>
      {% endif %}
  
      {# إذا كان نص الزر ورابط الزر موجودين #}
      {% if component.button_text and component.button_url %}
        <a href="{{ component.button_url }}" class="inline-block bg-primary text-white px-6 py-3 rounded-full shadow hover:bg-primary-dark transition">
          {{ component.button_text }}
        </a>
      {% endif %}
  
    </div>
</section>

